"use client";
import brandData from "./brandData";

// Render nothing until real partner brand data is available.
const Brands = () => {
  if (brandData.length === 0) return null;

  return null;
};

export default Brands;
